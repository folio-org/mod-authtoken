{
  "description": "A signed JWT token. Can be a single dummy token, a API token, or a refresh and access token pair.",
  "type": "object",
  "properties": {
    "token": {
      "type": "string",
      "description": "A dummy token"
    },
    "refreshToken": {
      "type": "string",
      "description": "A refresh token"
    },
    "accessToken": {
      "type": "string",
      "description": "An access token"
    },
    "apiToken": {
      "type": "string",
      "description": "An API token"
    }
  },
  "additionalProperties": false
}
