{
    "description": "An API token request containing the perms it requires",
    "type": "object",
    "properties": {
      "permissions": {
        "type": "array",
        "description": "The permissions that the API token requires"
      }
    },
    "required": [
      "permissions"
    ],
    "additionalProperties": false
  }
  